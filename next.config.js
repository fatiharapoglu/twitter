/** @type {import('next').NextConfig} */

const nextConfig = {
    experimental: {
        serverActions: true,
    },
    images: {
        domains: ["nifemmkaxhltrtqltltq.supabase.co"],
    },
};

module.exports = nextConfig;
