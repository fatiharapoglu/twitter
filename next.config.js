/** @type {import('next').NextConfig} */

const nextConfig = {
    experimental: {
        appDir: true,
    },
    images: {
        domains: ["nifemmkaxhltrtqltltq.supabase.co"],
    },
};

module.exports = nextConfig;
